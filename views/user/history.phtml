<div class="container my-5">
    <div class="bg-white p-4 rounded shadow">
        <h1 class="text-dark fw-bold mb-4">My Rentals</h1>

        <!-- Active Rentals Section -->
        <h4 class="text-dark fw-bold mb-3">Active Rentals</h4>
        <?php if (count($ongoingRentals) > 0): ?>
            <table class="table table-bordered table-hover">
                <thead class="table-success">
                    <tr>
                        <th>Transaction ID</th>
                        <th>Address</th>
                        <th>Start Date</th>
                        <th>Return Date</th>
                        <th>Rental Price</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($ongoingRentals as $rental): ?>
                        <tr>
                            <td class="text-success fw-bold">#<?= htmlspecialchars($rental['RentalTransactionId']) ?></td>
                            <td>
                                <a href="/equipment.php?id=<?= htmlspecialchars($rental['EquipmentId']) ?>" class="text-primary text-decoration-underline">
                                    <?= htmlspecialchars($rental['Equipment']['Name']) ?>
                                </a>
                            </td>
                            <td><?= htmlspecialchars(date('d/m/Y', strtotime($rental['PickUpDate']))) ?></td>
                            <td><?= htmlspecialchars($rental['ReturnDate'] ? date('d/m/Y', strtotime($rental['ReturnDate'])) : 'N/A') ?></td>
                            <td><?= number_format($rental['Equipment']['RentalPrice'], 2) ?></td>
                            <td>
                                <?php if ($rental['TransactionStatus'] === 'Overdue'): ?>
                                    <span class="badge bg-danger"><?= htmlspecialchars($rental['TransactionStatus']) ?></span>
                                <?php else: ?>
                                    <span class="badge bg-success"><?= htmlspecialchars($rental['TransactionStatus']) ?></span>
                                <?php endif; ?>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        <?php else: ?>
            <p class="text-muted">Nothing to show here...</p>
        <?php endif; ?>

        <!-- Rental History Section -->
        <h4 class="text-dark fw-bold mt-5 mb-3">Rental History</h4>
        <?php if (!empty($rentalHistory)): ?>
            <table class="table table-bordered table-hover">
                <thead class="table-success">
                    <tr>
                        <th>Record ID</th>
                        <th>Equipment</th>
                        <th>Pickup Date</th>
                        <th>Return Date</th>
                        <th>Return Condition</th>
                        <th>Total Cost</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($rentalHistory as $rental): ?>
                        <tr>
                            <td class="text-success fw-bold">#<?= htmlspecialchars($rental['ReturnRecordId']) ?></td>
                            <td><?= htmlspecialchars($rental['RentalTransaction']['Equipment']['Name'] ?? 'N/A') ?></td>
                            <td><?= htmlspecialchars(date('d/m/Y', strtotime($rental['RentalTransaction']['PickUpDate']))) ?></td>
                            <td><?= htmlspecialchars($rental['ActualReturnDate'] ? date('d/m/Y', strtotime($rental['ActualReturnDate'])) : 'N/A') ?></td>
                            <td><?= htmlspecialchars($rental['ReturnCondition']) ?></td>
                            <td>
                                <?php
                                $rentalTransactionId = $rental['RentalTransaction']['RentalTransactionId'] ?? null;
                                $lateFee = $lateFeeList[$rentalTransactionId] ?? 0;
                                $totalFee = ($rental['RentalTransaction']['Equipment']['RentalPrice'] ?? 0) + $lateFee;
                                ?>
                                <?= number_format($totalFee, 2) ?>
                            </td>
                            <td>
                                <a href="/equipment.php?id=<?= htmlspecialchars($rental['RentalTransaction']['Equipment']['EquipmentId'] ?? '') ?>" class="text-primary text-decoration-underline">
                                    Add Review
                                </a>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        <?php else: ?>
            <p class="text-muted">Nothing to show here...</p>
        <?php endif; ?>
    </div>
</div>